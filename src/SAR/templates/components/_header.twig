<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
	<div class="navbar-header">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>
		{% if username is empty %}
		<a href="/login" class="navbar-toggle navbar-btn navbar-login btn btn-primary visible-xs">Login</a>
		{% endif %}
		<a class="navbar-brand" href="/"><strong class="hidden-xs" >MCIS</strong>Silabus &amp; SAR</a>
	</div>
	<ul class="nav navbar-top-links navbar-right">
		{% if username is not empty %}
			{% if matkuls is not empty and matkuls['0']|length > 1 %}
		<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#">
				<i class="fa fa-briefcase fa-fw"></i> Lessons <i class="fa fa-caret-down"></i>
			</a>
			<ul class="dropdown-menu dropdown-tasks">
				{% for matkul in matkuls %}
				<li>
					<a href="/matakuliah/{{ matkul.KDMataKuliah }}">
						<div>
							<p>
								<strong>{{ matkul.NamaMK }}</strong>
								<span class="pull-right text-muted">{{ matkul.percentage }}% Complete</span>
							</p>
							<div class="progress progress-striped active">
                  <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="{{ matkul.percentage }}" aria-valuemin="0" aria-valuemax="100" style="width: 40%">
                      <span class="sr-only">{{ matkul.percentage }}% Complete (success)</span>
                  </div>
              </div>
						</div>
					</a>
				</li>
					{% if loop.index0 is even %}
				<li class="divider"></li>
					{% endif %}
				{% endfor %}
			</ul>
			<!-- /.dropdown-tasks -->
		</li>
			{% elseif matkuls is not empty %}
		<li>
			<div class="progress-navbar">
				<a href="/matakuliah/{{ matkuls.KDMataKuliah }}">
					<span class="progress-caption text-muted text-allcaps"><strong>{{ matkuls.NamaMK }}</strong></span>
					<div class="progress" style="width: 120px">
						<div class="progress-bar" role="progressbar" aria-valuenow="{{ matkuls.percentage }}" aria-valuemin="0" aria-valuemax="100" style="width: 40%">
							{{ matkuls.percentage }}%
						</div>
					</div>
				</a>
			</div>
		</li>
			{% endif %}
		<!-- /.dropdown -->
		<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#" title="{{ username }}">
				<i class="fa fa-user fa-fw"></i> {{ username|length > 15 ? username|slice(0, 15) ~ ' ...' : username }} <i class="fa fa-caret-down"></i>
			</a>
			<ul class="dropdown-menu dropdown-user">
				<li><a href="/user"><i class="fa fa-user fa-fw"></i> User Profile</a>
				</li>
				<li><a href="#"><i class="fa fa-gear fa-fw"></i> Settings</a>
				</li>
				<li class="divider"></li>
				<li><a href="/logout"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
				</li>
			</ul>
			<!-- /.dropdown-user -->
		</li>
		{% else %}
		<a href="/login" class="btn btn-primary primary navbar-btn btn-login hidden-xs">Login</a>
		{% endif %}
		<!-- /.dropdown -->
	</ul>
	{% include 'components/_sidebar.twig' %}
</nav>
